# Python 3.11 is required for the 3D rendering scripts (see note below).
# The simulation scripts themselves run on 3.11 or 3.12.

# The simulator. Not on PyPI: install from source.
#   git clone https://github.com/ssloxford/dsns
#   pip install -e dsns
# The results reported in the thesis were produced against DSNS as of commit
# 7e3e4f045ae3183aeacfce41b65ea9e275b9d29f (18 July 2026).

networkx==3.6.1
numpy==1.26.4

# 3D rendering only (visualize_*.py). The pyglet pin matters: pyglet 2.x breaks
# the offscreen renderer, producing cropped or empty images, so the rendering
# scripts must run under Python 3.11 with pyglet 1.5.x.
pyrender==0.1.45
trimesh==4.11.3
imageio==2.37.3
pyglet==1.5.27
